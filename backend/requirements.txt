# Step 1 — schema + policy loader. Later steps append their own deps.
SQLAlchemy>=2.0.30
alembic>=1.13
psycopg[binary]>=3.1
pydantic>=2.7
pydantic-settings>=2.3
PyYAML>=6.0
pytest>=8.2
pytest-cov>=5.0

# Step 2 — intake API. httpx backs starlette's TestClient.
fastapi>=0.115
uvicorn[standard]>=0.30
httpx>=0.27

# Step 4 — certificate and config collectors. crossplane parses nginx.conf as
# nginx itself does; SPEC.md 7.4 rules out doing it with regex.
cryptography>=42.0
crossplane>=0.5.8

# Step 11 - code and binary collectors. Semgrep is invoked as a subprocess with
# --json and a local rule file only; pyelftools reads ELF headers, .dynsym and
# .rodata without executing anything.
semgrep>=1.100
pyelftools>=0.31

# Step 12 - CBOM import and export. Typed CycloneDX 1.6 model in both
# directions; the validation extra carries the JSON schema the export is checked
# against before it is served.
cyclonedx-python-lib[validation]>=11.0

# Step 7 - network probe. Imported as a library, not shelled out to: SPEC.md 7.5
# wants typed ServerScanResult objects rather than a JSON round-trip. Pulls in
# nassl, which pins cryptography below the newest release.
sslyze>=6.0
