CLAUDE.md
BUILD_PLAN.md

# Python
.venv/
__pycache__/
*.py[cod]
.pytest_cache/

# Local runtime artefacts
.env
*.sqlite

# Working notes
notes/

# Demo environment artefacts (demo/README.md). Certificates carry private keys
# and a key in a git history is a real incident even when the key is a toy;
# the ELF is a build output. Both are reproducible from demo/gen_certs.sh and
# demo/cbin/Makefile.
demo/certs/
demo/cbin/build/

# Coverage data from pytest-cov
.coverage
.coverage.*
htmlcov/

# Vendored native libraries for local tooling (WeasyPrint needs Pango on Windows)
.tools/

# Local list of repositories to test against
repo_links.txt
